[tool:pytest]
testpaths = test
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --verbose
    --tb=short
    --cov=src.bot
    --cov-report=term-missing
    --cov-report=html
    --cov-report=xml
    --cov-fail-under=100
asyncio_mode = auto 